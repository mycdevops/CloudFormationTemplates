This repository contains working codes for CloudFormation Templates in AWS Free Tier.

CFTwithALB.json - Creates key pairs, Security Groups with port 80 Allow, two instances with Web server with index page giving Webserver1 and Webserver2 as output with Automatic Load Balancers.  Writes the DNS name of the ALB to the output.

